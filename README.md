1) Within a GitHub Action that runs whenever code is pushed because it automatically checks the code every time someone updates the project. This helps catch bugs early before broken code gets merged or shared with the team. It also makes testing more consistent because everyone’s code is tested the same way. For the Recipe project, this would help make sure features like recipe cards, localStorage, and UI updates still work after new changes.
2) No
3) Navigation mode analyzes the website during the page load process, like loading speed and overall performance when first opening the page. Snapshot mode only analyzes the webpage in its current state after it has already loaded mainly focusing on accessibility and structure rather than performance or interactions.
4) Three things you could do to improve the CSE 110 shop site based on the Lighthouse results are:
- Add a meta description to improve SEO and content best practices.
- Add a `lang="en"` attribute to the `<html>` element to improve accessibility for screen readers.
- Minify and reduce unused JavaScript files to improve page load performance and reduce transfer size.





